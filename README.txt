completeRPC should run perfectly fine and includes exercises 3-6 of chapter 4. It was debugged and tested on Visual Studio 2017's local
Windows Debugger. Legal commands include: + * / - % swap dupe clear sin cos exp pow ?(prints top of stack) !(pushes most recent value
popped by \n) \n (pops the top of the stack and displays the value on the command window).
